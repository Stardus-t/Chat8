# Chat8

一个基于 C++ 和 Qt 开发的聊天系统，包含服务器端（ChatServer、ChatServer2、StatusServer、GateServer 等）和客户端（Client），支持用户登录、聊天消息发送与接收、好友添加、聊天记录存储等功能。


## 项目结构

- **ChatServer/**：聊天服务器实现，处理客户端连接、消息转发等核心逻辑。
- **ChatServer2/**：另一个版本的聊天服务器，包含 gRPC 服务实现。
- **StatusServer/**：状态服务器，提供生成唯一标识等功能。
- **GateServer/**：网关服务器，负责配置管理等。
- **Client/**：客户端程序，基于 Qt 开发，提供图形化界面，支持与服务器交互。


## 主要功能

1. **用户交互**：客户端支持登录、注册、个人信息修改等操作。
2. **消息通信**：支持公聊和私聊，可发送文本消息。
3. **好友管理**：支持添加好友功能。
4. **聊天记录**：服务器端会存储聊天历史记录，客户端可查询。
5. **多服务器协作**：通过网关、状态服务器等组件协同工作，提供更稳定的服务。


## 技术栈

- **服务器端**：C++、Boost 库（用于网络、配置解析等）、gRPC（用于服务间通信）、Redis（用于存储聊天记录等数据）、Asio（用于异步网络操作）。
- **客户端**：Qt（用于图形界面和网络通信）。
- **数据格式**：Protocol Buffers（用于消息序列化）。


## 环境依赖

- 编译器：支持 C++11 及以上标准的编译器（如 GCC、Clang、MSVC）。
- 第三方库：
  - Boost（1.60+）
  - Qt（5.12+）
  - gRPC（1.30+）
  - Protobuf（3.13+）
  - Redis 客户端库
- 运行环境：Windows、Linux 等操作系统。


## 编译与运行

### 服务器端（以 ChatServer2 为例）

1. 配置依赖库路径，确保 Boost、gRPC、Protobuf、Redis 等库已正确安装。
2. 修改配置文件 `config.ini`，设置服务器端口、Redis 连接信息等。
3. 使用合适的构建工具（如 CMake、Visual Studio）编译项目。
4. 运行生成的可执行文件：
